{
	"name": "dice-roller",
	"version": "0.1.0",
	"homepage": "https://arth-shukla.github.io/dice-roller",
	"private": true,
	"dependencies": {
		"@arth-shukla/my-icons": "^1.0.0",
		"@testing-library/jest-dom": "^5.16.4",
		"@testing-library/react": "^13.3.0",
		"@testing-library/user-event": "^13.5.0",
		"bootstrap": "^5.2.0",
		"react": "^18.2.0",
		"react-bootstrap": "^2.4.0",
		"react-dom": "^18.2.0",
		"react-scripts": "5.0.1",
		"web-vitals": "^2.1.4"
	},
	"scripts": {
		"full-deploy": "npm run prettier && npm run deploy && git add -A && git commit -m \"full-deploy\"",
		"prettier": "prettier src --write",
		"deploy-commit": "npm run deploy && git add --all && git commit -m \"deploy-commit\"",
		"predeploy": "npm run build",
		"deploy": "gh-pages -d build",
		"start": "react-scripts start",
		"build": "react-scripts build",
		"test": "react-scripts test",
		"eject": "react-scripts eject"
	},
	"eslintConfig": {
		"extends": [
			"react-app",
			"react-app/jest"
		]
	},
	"browserslist": {
		"production": [
			">0.2%",
			"not dead",
			"not op_mini all"
		],
		"development": [
			"last 1 chrome version",
			"last 1 firefox version",
			"last 1 safari version"
		]
	},
	"devDependencies": {
		"gh-pages": "^4.0.0",
		"sass": "^1.54.0"
	}
}
